# NPM Configuration for brave-real-browser

# Always install latest versions when "latest" is specified
save-exact=false

# Automatically update dependencies to compatible versions
save-prefix=^

# Use package-lock.json for reproducible installs
package-lock=true

# Log level for dependency updates
loglevel=info
